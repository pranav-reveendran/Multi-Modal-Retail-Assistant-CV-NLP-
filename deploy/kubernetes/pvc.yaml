apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retail-data-pvc
  labels:
    app: retail-assistant
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard  # Change based on your cluster
